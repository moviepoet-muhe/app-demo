/* index.wxss */
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh; /* 视口高度 */
}

.title {
  font-size: 24px;
  margin-bottom: 20px;
}

.content {
  font-size: 18px;
  margin-bottom: 20px;
}